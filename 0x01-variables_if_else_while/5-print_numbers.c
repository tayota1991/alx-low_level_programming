#include<stdio.h>
#include<ctype.h>
/**
 * main - Entry point
 *
 * Description: 'putchar function'
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
